# Developer / test-only dependencies (B11).
# Production deps live in requirements.txt; keep this file out of the runtime
# image. Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
